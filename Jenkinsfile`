pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build successful!'
            }
        }

        stage('Test') {
            steps {
                echo 'Test successful!'
            }
        }

        stage('Deploy') {
            steps {
                sh 'sudo cp index.html /var/www/html/index.html'
            }
        }
    }
}
